# Core Configuration
API_KEY=your-api-key-will-be-used-api-calls
RATE_LIMIT_PER_HOUR=10  # Optional: Requests per hour per IP (default: 10)

# Upstash Redis Configuration (Optional)
# Leave blank to use in-memory rate limiting (localhost only, doesn't work on Vercel serverless)
# Set these for distributed rate limiting on Vercel:
# UPSTASH_REDIS_REST_URL=https://your-upstash-redis-url.upstash.io
# UPSTASH_REDIS_REST_TOKEN=your-upstash-redis-token